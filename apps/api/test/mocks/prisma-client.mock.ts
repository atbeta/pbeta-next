// Stub for Prisma generated client — used in unit tests only.
// Real tests (E2E) use the actual generated client via real DB.
export class PrismaClient {
  $connect = jest.fn()
  $disconnect = jest.fn()
  $transaction = jest.fn()
}
